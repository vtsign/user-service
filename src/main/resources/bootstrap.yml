spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://vtsign.tech:8888
server:
  port: 8400
  servlet:
    context-path: /user
  error:
    whitelabel:
      enabled: false
springdoc:
  version: '1.0'
tech:
  vtsign:
    hostname: http://localhost:8765
    kafka:
      user-service:
        register: 'user-service-register'